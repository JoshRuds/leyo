int x = 5 @ 3;
int y = 7 # 2;
int z = 10 $ 4;
return x & y;

string a = "this string never ends;

string b = "bad escapes: \q \x \z \9";

if (a = = b) {
    x = 1;
}

int a = 5
int b = 6
int c = a + b

int n1 = 12abc;
int n2 = 3..5;
int n3 = 0012xyz;

func main() {
    int x = 5 $ 6;
    string s = "ok
    return x & y;
}

if (x > 5 {
    y = 3;
}

array a = [1, 2, 3;

!!! @@ ### $$$ %%%
int main = 5;

int result = (5 + (3 * (2 + (1 - 0))));

int broken = (5 + (3 * (2 + 1)));

string s2 = "ok\nstill valid but messy";

if (result == 29 {
    result = result + 1;
}