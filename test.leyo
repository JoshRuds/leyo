int x = 5;
float y = 3.14;
string s = "hello world";

~ this is a comment ~

if (x >= 5) {
    y = y + 1;
}

a === b;
c == d;
e != f;

bad1 = 12..34;
bad2 = "unterminated

weird = @#$;

array nums = [1, 2, 3, 4];

func main() {
    int result = (5 + (3 * (2 + 1)));
    string msg = "ok";
    return result;
}